{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against React App",
            "url": "http://localhost:3000", // Update the URL to match your React app's port
            "webRoot": "${workspaceFolder}"
        }
    ]
}
